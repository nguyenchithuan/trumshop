import type { HomeCopy, Language } from "@/features/home/components/HomePage";

interface ContactSectionProps { readonly contacts: Readonly<Record<string, string>>; readonly content: HomeCopy; readonly language: Language; readonly onAdvice: () => void; readonly onToast: () => void; readonly supportHours: Readonly<Record<Language, string>>; }

export default function ContactSection({ contacts, content, language, onAdvice, onToast, supportHours }: ContactSectionProps) {
  return <section className="contact-section section shell scroll-reveal" id="lien-he"><div className="contact-glow" aria-hidden="true" /><div className="contact-grid"><div className="contact-copy"><p className="section-kicker">{content.contactLabel}</p><h2>{content.contactTitle}</h2><p>{content.contactText}</p><div className="contact-plan-chips"><span>GO</span><span>PLUS</span><span>PRO</span><span>BUSINESS</span></div></div><div className="contact-panel"><div className="contact-status"><span className="status-dot" />{content.support} {supportHours[language]}</div><div className="contact-list">{[["Zalo", contacts.zalo], ["Facebook", contacts.facebook], ["Instagram", contacts.instagram], ["Hotline", contacts.hotline], ["Email", contacts.email]].map(([label, value]) => <div key={label}><span>{label}</span><strong>{value || content.updating}</strong></div>)}</div><div className="contact-buttons"><button type="button" onClick={onAdvice}>{content.getAdvice}<span>↗</span></button><button className="muted-action" type="button" onClick={onToast}>{content.callHotline}</button></div></div></div></section>;
}
